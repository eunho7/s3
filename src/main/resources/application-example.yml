# 환경별 profile의 기본 템플릿 예시
spring:
  datasource:
    url: jdbc:mysql://<HOST>:3306/<DB_NAME>?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: <USERNAME>
    password: <PASSWORD>

aws:
  s3:
    bucket: <S3_BUCKET_NAME>
    region: ap-northeast-2
    presigned-url-duration-minutes: 15
